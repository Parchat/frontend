services:
  frontend-dev:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "3001:3000"
    volumes:
      - .:/home/frontend-dev
      - /home/frontend-dev/node_modules
    environment:
      - NODE_ENV=development